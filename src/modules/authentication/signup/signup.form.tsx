// import {useForm} from "react-hook-form";

export const SignupForm = () => {
    // const {handleSubmit, control, formState: {errors}, register, setError, reset,} = useForm()
    return (
        <>
            formulaire
        </>
    )
}